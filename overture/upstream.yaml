version: '3'

services:
  overture:
    image: ghcr.io/apocalypsor/overture:latest:upstream
    container_name: smartdns
    network_mode: "host"
    volumes:
      - ./conf:/smartdns
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
          max-size: "128k"
